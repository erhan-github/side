[build]
builder = "nixpacks"

[deploy]
startCommand = "npm start -- -p $PORT -H 0.0.0.0"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
healthcheckStartPeriod = 30
restartPolicyType = "ON_FAILURE"
